#include <RcppArmadillo.h>
using namespace std;

void parse_split_vector(Rcpp::NumericVector,
                        arma::icolvec&,
                        arma::icolvec&,
                        arma::icolvec&);
